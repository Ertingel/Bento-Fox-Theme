
{% if section.settings.collection %}
	{% render 'collection' with 
		title:section.settings.title 
		collection:section.settings.collection
		url: collection.url
	%}
{% else %}
	{% render 'collection' with 
		title:section.settings.title 
	%}
{% endif %}

{% schema %}
{
	"name": "Collection",
	"settings": [
		{
			"type": "text",
			"id": "title",
			"label": "Title"
		},
		{
			"type": "collection",
			"id": "collection",
			"label": "Collection"
		}
	],
	"presets": [
      {
        "name": "Collection",
        "category": "Body"
      }
    ]
}
{% endschema %}